pipeline {
    agent any
    stages {
        stage('Checkout SCM') {
            steps {
                checkout scm
            }
        }
        stage('List files in workspace') {
            steps {
                sh 'ls -la'
            }
        }
        stage('Build') {
          
        }
        stage('Test') {
        
        }
        stage('Deploy to Tomcat') {
          
        }
    }
}
